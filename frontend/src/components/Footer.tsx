const Footer = () => {
    return (
        <footer className="text-center text-gray-500 py-8 border-t border-gray-800">
            © {new Date().getFullYear()} Paceify
        </footer>
    );
};

export default Footer;